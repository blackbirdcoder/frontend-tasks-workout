# HW 23. Implement the removeElement function
Implement the removeElement(array, item) function to remove the item element from the array array.

For example:
```javascript
const array = [1, 2, 3, 4, 5, 6, 7];
removeElement(array, 5 );
console.log(array);
// Результат: [1, 2, 3, 4, 6, 7]
```
